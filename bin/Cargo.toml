[package]
name = "zippyst-cmd"
version = "0.1.4"
authors = ["Scotow <contact@scotow.com>"]
edition = "2021"

[dependencies]
zippyst = { path = "../lib", features = ["fetch"] }
tokio = { version = "1.20.4", default-features = false, features = ["macros", "rt-multi-thread"] }
